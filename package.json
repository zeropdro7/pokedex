{
  "name": "pokedex",
  "version": "1.0.0",
  "private": true,
  "description": "This project is the boilerplate for Pokedex monorepo!!",
  "main": "index.js",
  "workspaces": [
    "poke-web"
  ],
  "scripts": {
    "start:web": "npm run start --workspace=poke-web",
    "build:web": "npm run build --workspace=poke-web",
    "test:web": "npm run test --workspace=poke-web",
    "lint": "eslint ./poke-web"
  },
  "eslintConfig": {
    "ignorePatterns": "build",
    "extends": [
      "react-app",
      "react-app/jest"
    ],
    "env": {
      "jest": true
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
